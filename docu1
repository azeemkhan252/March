node {
    stage 'cloning the git repository'
    git 'https://github.com/bhiapp4/crudApp.git'
}
pipeline {
  agent any
  tools {
    maven 'maven' install
  }
  stages {
    stage('building artifact') {dirextory3
      steps {
        sh 'mvn clean test package' added to file1
      }
    }
  }
}
