https://github.com/azeemkhan252/March.git
node {
    stage 'cloning the git repository'
    git 'https://github.com/bhiapp4/crudApp.git'
}
pipeline {something in the pipeline
  agent any
  tools {
    maven 'maven'set repository
  }
  stages { 10 stage
    stage('building artifact') {
      steps {
        sh 'mvn clean test package'
      }
    }
  }
}
